using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OpenWall : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        /*
        if (diningKeyCollected)
        {
            Destroy(MoveWall1);
        }
        */
    }
}
